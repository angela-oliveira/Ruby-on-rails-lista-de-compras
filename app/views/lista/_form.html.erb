<%= form_with(model: listum) do |form| %>
  <% if listum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(listum.errors.count, "error") %> Este item não pode ser salvo:</h2>

      <ul>
        <% listum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
